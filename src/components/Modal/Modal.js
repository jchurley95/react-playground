import React, { Component } from 'react';
import { ModalContainer } from './Styled/Modal.styled';

class Modal extends Component {
    render() {
        return (
            <ModalContainer>
                
            </ModalContainer>
        );
    }
}

export default Modal;