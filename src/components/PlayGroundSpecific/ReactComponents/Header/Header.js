import React from 'react';
import HeaderContainer from '../../StyledComponents/Header.styled';

const Header = (props) => {
    return (
        <HeaderContainer>
            <h1>React Playground</h1>
        </HeaderContainer>
    );
};

export default Header;